{
    "configurations": [
        {
            "name": "Docker Node.js Launch",
            "type": "docker",
            "request": "launch",
            "preLaunchTask": "docker-run: debug",
            "platform": "node",
            "node": {
                "package": "${workspaceFolder}/src/apps/apis/process-handler/package.json",
                "localRoot": "${workspaceFolder}/src/apps/apis/process-handler"
            }
        }
    ]
}